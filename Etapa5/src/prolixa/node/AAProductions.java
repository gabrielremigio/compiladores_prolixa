/* This file was generated by SableCC (http://www.sablecc.org/). */

package prolixa.node;

import java.util.*;
import prolixa.analysis.*;

@SuppressWarnings("nls")
public final class AAProductions extends PAProductions
{
    private final LinkedList<PAProd> _prods_ = new LinkedList<PAProd>();

    public AAProductions()
    {
        // Constructor
    }

    public AAProductions(
        @SuppressWarnings("hiding") List<?> _prods_)
    {
        // Constructor
        setProds(_prods_);

    }

    @Override
    public Object clone()
    {
        return new AAProductions(
            cloneList(this._prods_));
    }

    @Override
    public void apply(Switch sw)
    {
        ((Analysis) sw).caseAAProductions(this);
    }

    public LinkedList<PAProd> getProds()
    {
        return this._prods_;
    }

    public void setProds(List<?> list)
    {
        for(PAProd e : this._prods_)
        {
            e.parent(null);
        }
        this._prods_.clear();

        for(Object obj_e : list)
        {
            PAProd e = (PAProd) obj_e;
            if(e.parent() != null)
            {
                e.parent().removeChild(e);
            }

            e.parent(this);
            this._prods_.add(e);
        }
    }

    @Override
    public String toString()
    {
        return ""
            + toString(this._prods_);
    }

    @Override
    void removeChild(@SuppressWarnings("unused") Node child)
    {
        // Remove child
        if(this._prods_.remove(child))
        {
            return;
        }

        throw new RuntimeException("Not a child.");
    }

    @Override
    void replaceChild(@SuppressWarnings("unused") Node oldChild, @SuppressWarnings("unused") Node newChild)
    {
        // Replace child
        for(ListIterator<PAProd> i = this._prods_.listIterator(); i.hasNext();)
        {
            if(i.next() == oldChild)
            {
                if(newChild != null)
                {
                    i.set((PAProd) newChild);
                    newChild.parent(this);
                    oldChild.parent(null);
                    return;
                }

                i.remove();
                oldChild.parent(null);
                return;
            }
        }

        throw new RuntimeException("Not a child.");
    }
}
