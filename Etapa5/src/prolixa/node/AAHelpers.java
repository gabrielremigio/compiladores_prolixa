/* This file was generated by SableCC (http://www.sablecc.org/). */

package prolixa.node;

import java.util.*;
import prolixa.analysis.*;

@SuppressWarnings("nls")
public final class AAHelpers extends PAHelpers
{
    private final LinkedList<PAHelperDef> _defs_ = new LinkedList<PAHelperDef>();

    public AAHelpers()
    {
        // Constructor
    }

    public AAHelpers(
        @SuppressWarnings("hiding") List<?> _defs_)
    {
        // Constructor
        setDefs(_defs_);

    }

    @Override
    public Object clone()
    {
        return new AAHelpers(
            cloneList(this._defs_));
    }

    @Override
    public void apply(Switch sw)
    {
        ((Analysis) sw).caseAAHelpers(this);
    }

    public LinkedList<PAHelperDef> getDefs()
    {
        return this._defs_;
    }

    public void setDefs(List<?> list)
    {
        for(PAHelperDef e : this._defs_)
        {
            e.parent(null);
        }
        this._defs_.clear();

        for(Object obj_e : list)
        {
            PAHelperDef e = (PAHelperDef) obj_e;
            if(e.parent() != null)
            {
                e.parent().removeChild(e);
            }

            e.parent(this);
            this._defs_.add(e);
        }
    }

    @Override
    public String toString()
    {
        return ""
            + toString(this._defs_);
    }

    @Override
    void removeChild(@SuppressWarnings("unused") Node child)
    {
        // Remove child
        if(this._defs_.remove(child))
        {
            return;
        }

        throw new RuntimeException("Not a child.");
    }

    @Override
    void replaceChild(@SuppressWarnings("unused") Node oldChild, @SuppressWarnings("unused") Node newChild)
    {
        // Replace child
        for(ListIterator<PAHelperDef> i = this._defs_.listIterator(); i.hasNext();)
        {
            if(i.next() == oldChild)
            {
                if(newChild != null)
                {
                    i.set((PAHelperDef) newChild);
                    newChild.parent(this);
                    oldChild.parent(null);
                    return;
                }

                i.remove();
                oldChild.parent(null);
                return;
            }
        }

        throw new RuntimeException("Not a child.");
    }
}
