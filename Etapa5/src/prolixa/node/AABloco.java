/* This file was generated by SableCC (http://www.sablecc.org/). */

package prolixa.node;

import java.util.*;
import prolixa.analysis.*;

@SuppressWarnings("nls")
public final class AABloco extends PABloco
{
    private final LinkedList<PADeclaracao> _dclrs_ = new LinkedList<PADeclaracao>();
    private final LinkedList<PAComando> _cmds_ = new LinkedList<PAComando>();

    public AABloco()
    {
        // Constructor
    }

    public AABloco(
        @SuppressWarnings("hiding") List<?> _dclrs_,
        @SuppressWarnings("hiding") List<?> _cmds_)
    {
        // Constructor
        setDclrs(_dclrs_);

        setCmds(_cmds_);

    }

    @Override
    public Object clone()
    {
        return new AABloco(
            cloneList(this._dclrs_),
            cloneList(this._cmds_));
    }

    @Override
    public void apply(Switch sw)
    {
        ((Analysis) sw).caseAABloco(this);
    }

    public LinkedList<PADeclaracao> getDclrs()
    {
        return this._dclrs_;
    }

    public void setDclrs(List<?> list)
    {
        for(PADeclaracao e : this._dclrs_)
        {
            e.parent(null);
        }
        this._dclrs_.clear();

        for(Object obj_e : list)
        {
            PADeclaracao e = (PADeclaracao) obj_e;
            if(e.parent() != null)
            {
                e.parent().removeChild(e);
            }

            e.parent(this);
            this._dclrs_.add(e);
        }
    }

    public LinkedList<PAComando> getCmds()
    {
        return this._cmds_;
    }

    public void setCmds(List<?> list)
    {
        for(PAComando e : this._cmds_)
        {
            e.parent(null);
        }
        this._cmds_.clear();

        for(Object obj_e : list)
        {
            PAComando e = (PAComando) obj_e;
            if(e.parent() != null)
            {
                e.parent().removeChild(e);
            }

            e.parent(this);
            this._cmds_.add(e);
        }
    }

    @Override
    public String toString()
    {
        return ""
            + toString(this._dclrs_)
            + toString(this._cmds_);
    }

    @Override
    void removeChild(@SuppressWarnings("unused") Node child)
    {
        // Remove child
        if(this._dclrs_.remove(child))
        {
            return;
        }

        if(this._cmds_.remove(child))
        {
            return;
        }

        throw new RuntimeException("Not a child.");
    }

    @Override
    void replaceChild(@SuppressWarnings("unused") Node oldChild, @SuppressWarnings("unused") Node newChild)
    {
        // Replace child
        for(ListIterator<PADeclaracao> i = this._dclrs_.listIterator(); i.hasNext();)
        {
            if(i.next() == oldChild)
            {
                if(newChild != null)
                {
                    i.set((PADeclaracao) newChild);
                    newChild.parent(this);
                    oldChild.parent(null);
                    return;
                }

                i.remove();
                oldChild.parent(null);
                return;
            }
        }

        for(ListIterator<PAComando> i = this._cmds_.listIterator(); i.hasNext();)
        {
            if(i.next() == oldChild)
            {
                if(newChild != null)
                {
                    i.set((PAComando) newChild);
                    newChild.parent(this);
                    oldChild.parent(null);
                    return;
                }

                i.remove();
                oldChild.parent(null);
                return;
            }
        }

        throw new RuntimeException("Not a child.");
    }
}
